Mover1(Init){

* (Init): 
	| Cell(N,G) ? Wizz :(Init)
	| Cell(E,G) ? Wizz :(Init)
	| Cell(O,G) ? Wizz :(Init)
	| Cell(S,G) ? Wizz :(Init)
    | Cell(N,V) ? Move(N) :(Nord)
    | Cell(E,V) ? Move(E) :(Est)
    | Cell(O,V) ? Move(O) :(Ouest)
    | Cell(S,V) ? Move(S) :(Sud)
    | Cell(S,P) ? Pick(S) & Move(S) :(Init)
    | Cell(N,P) ? Pick(N) & Move(N) :(Init)	
    | Cell(E,P) ? Pick(E) & Move(E) :(Init)
    | Cell(O,P) ? Pick(O) & Move(O) :(Init)
        
}

Mover2(Init){

*(Init): Closest ? Follow :(Init)
}
